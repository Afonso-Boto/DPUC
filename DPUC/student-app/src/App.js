import { Section, SectionHeader } from "@uaveiro/ui";

function App() {
  return (
    <div className="App">
      <Section backgroundColor="#e0e0e0">
        <SectionHeader hasLink={true} hasLine={true} title="Student App" textColor="#000" />
      </Section>
    </div>
  );
}

export default App;
